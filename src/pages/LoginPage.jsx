import { Typography, Box } from "@mui/material";

export const LoginPage = () => (
  <Box>
    <Typography variant="h4" gutterBottom>Регистрация</Typography>
    <Typography variant="body1">Здесь будет регистрация / авторизация</Typography>
  </Box>
);